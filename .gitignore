# Node modules
node_modules/
dist/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/
.env

# Engine binaries (they are large and shouldn't be in git)
engine/
*.exe
*.dll
*.pb.gz

# Logs
*.log

# OS generated files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# Scratch folder
scratch/

# Large binaries
*.whl

games_of_derdiedasdie/
cszero_kaggle_data.zip

# LaTeX build artifacts
*.aux
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.stdout

# Rendered page previews under docs/ (regenerate with:
#   pdftoppm -r 110 -png <file>.pdf page)
# All figures are TikZ, so no document depends on a committed image.
docs/design_session/**/*.png

# LaTeX console captures (regenerable)
docs/design_session/**/build*.txt
docs/design_session/**/_probe.pdf

# generated / large artifacts (added 2026-08-19)
downloads/
kaggle_files/
profiles/

# Application material lives in the private job_search repo, never here.
applications/

# Timetable daemon runtime state (per-second cursor + local adherence log)
trainer/state/schedule_daemon.json
trainer/state/schedule_daemon.tmp
trainer/state/schedule_log.jsonl
dist/
