# (empty — awaiting the returned report)

Drop the research agent's returned document here, as `REPORT.md`, then delete this placeholder.

Before it is used for anything: resolve every DOI, re-fetch every quotation against its source, and
record the verdict in `agents/ACTIVE.md`. Targets marked UNVERIFIED need no audit — they are
already correct. Targets answered confidently are where fabrication hides.
