import * as compat from 'chessops/compat';
import * as chess from 'chessops/chess';
import * as fen from 'chessops/fen';
import * as util from 'chessops/util';
import * as pgn from 'chessops/pgn';
import * as chessground from 'chessground';

console.log("compat:", Object.keys(compat));
console.log("chess:", Object.keys(chess));
console.log("fen:", Object.keys(fen));
console.log("util:", Object.keys(util));
console.log("pgn:", Object.keys(pgn));
console.log("chessground:", Object.keys(chessground));
